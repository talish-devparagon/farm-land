---
description: Set up a worktree and start working on a new task
argument-hint: [branch-name] [task description]
---

1. Create a worktree: git worktree add ../<branch-name> -b <branch-name>
2. cd into it
3. Start working on: $ARGUMENTS
4. When done, commit, push, and open a PR — don't merge to main directly
